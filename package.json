{
	"name": "@0xver/shell",
	"license": "GPL-3.0-only",
	"version": "3.0.4",
	"description": "Web3 focused local boilerplate library",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/0xver/shell.git"
	},
	"main": "src/shell.js",
	"scripts": {
		"lint": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --write ."
	},
	"bin": {
		"shell": "src/shell.js"
	},
	"devDependencies": {
		"prettier": "^2.8.2",
		"prettier-plugin-solidity": "^1.1.1",
		"prettier-plugin-svelte": "^2.9.0"
	}
}
