{
	"name": "@0xver/shell",
	"license": "GPL-3.0-only",
	"version": "4.0.0",
	"description": "Install repos in the 0xver ecosystem with ease",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/0xver/shell.git"
	},
	"main": "src/shell.js",
	"scripts": {
		"lint": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --write ."
	},
	"bin": {
		"shell": "src/shell.js"
	},
	"devDependencies": {
		"prettier": "^2.8.4"
	}
}
